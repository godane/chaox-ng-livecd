# Contributor: Jens Pranaitis <jens@chaox.net>
# Contributor: Thomas Baechler <thomas@archlinux.org>
# Contributor: Aaron Griffin <aaron@archlinux.org>
pkgname=initscripts-chaox
pkgver=2009.03
pkgrel=2
pkgdesc="System initialization/bootup scripts"
arch=('i686' 'x86_64')
url="http://www.archlinux.org"
license=('GPL')
groups=('base')
backup=(etc/inittab etc/rc.conf etc/rc.local etc/rc.local.shutdown)
depends=('glibc' 'bash' 'awk' 'grep' 'coreutils' 'sed' 'udev>=118' 'net-tools' 'ncurses' 'kbd' 'findutils')
conflicts=("initscripts")
provides=("initscripts")
source=(ftp://ftp.archlinux.org/other/initscripts/initscripts-${pkgver}-2.tar.gz chaox.patch)
md5sums=('2fc63314e39e7d0d18e03a1a4e44771d'
         '6cdcd7f0322ec3f53f90972a7a2a596e')
build() {
  cd "${srcdir}"/initscripts-${pkgver}-2/
  patch -Np0 -i "${srcdir}"/chaox.patch || return 1
  DESTDIR="${pkgdir}" ./install.sh || return 1
}
