# $Id$
# Maintainer: Aaron Griffin <aaron@archlinux.org>

pkgname=chaox-installer
pkgver=2009.02
pkgrel=1
pkgdesc="The chaox install script"
arch=('i686' 'x86_64')
url="http://projects.archlinux.org/?p=installer.git"
license=('GPL')
depends=('bash')
source=("ftp://ftp.archlinux.org/other/archlinux-installer/archlinux-installer-$pkgver.tar.gz" "chaox-setup.diff")

build() {
  cd "$srcdir/archlinux-installer-$pkgver"
  patch -p0 < "$srcdir/chaox-setup.diff" || return 1
  install -D -m755 setup "$pkgdir/arch/chaox-setup"
}

# vim:set ts=2 sw=2 et:
