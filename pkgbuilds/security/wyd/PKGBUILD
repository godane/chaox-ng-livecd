# Contributor: Jens Pranaitis <jens@chaox.net>
# Contributor: nofxx <x@nofxx.com>
pkgname=wyd
pkgver=0.2
pkgrel=1
pkgdesc="Gets keywords from personal files and creates wordlists based on them"
arch=('i686' 'x86_64')
url="http://www.remote-exploit.org/codes/wyd"
license=('GPL')
depends=('perl' 'perl-html-parser' 'catdoc' 'perl-openoffice-oodoc' 'mp3info' 'jhead')
source=(http://www.remote-exploit.org/codes/wyd/$pkgname-$pkgver.tar.gz "wyd")
md5sums=('a032c851e10a9eb102f5196c27b8e6b0'
         'd90e8330c8ca0013a39457cd57b04b4e')
build() {
	cd "${srcdir}"/$pkgname-$pkgver
	install -d "${pkgdir}"/usr/share/wyd || return 1
	install -d "${pkgdir}"/usr/bin || return 1
	cp -r * "${pkgdir}"/usr/share/wyd || return 1
	chmod +x "${pkgdir}"/usr/share/wyd/wyd.pl || return 1
  install -m 755 "${srcdir}"/wyd "${pkgdir}"/usr/bin/ || return 1
}
